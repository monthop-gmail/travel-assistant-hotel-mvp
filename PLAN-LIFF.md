## 🎯 เป้าหมาย: เพิ่ม LIFF ให้ `travel-assistant-hotel-mvp`

### ✅ สิ่งที่จะทำ
- สร้าง `liff.html` หน้าเริ่มต้นสำหรับ LINE (โหลด LIFF SDK, ตรวจสอบ context, redirect ไป `/`)
- ปรับ `index.html`: เพิ่มการตรวจจับ LIFF context และแสดง UI ที่เหมาะสม (เช่น ปุ่ม "แชร์ตำแหน่ง" แทน "ใช้พิกัดปัจจุบัน" หากใน LINE)
- อัปเดต `README.md`: เพิ่ม section "LIFF Setup" พร้อมคำสั่งตั้งค่าใน LINE Developers Console

### 📁 ไฟล์ที่เกี่ยวข้อง
- `/liff.html` (ใหม่)
- `/index.html` (แก้ไข)
- `/README.md` (แก้ไข)

### ⚙️ การตั้งค่า LIFF (ไม่ต้องทำผ่าน bot — แจ้งไว้ใน README)
- URL: `https://<your-domain>/liff.html`
- Features: `geolocation`, `openExternalBrowser` (สำหรับ deep link ไป provider)
- ตั้งค่าใน [LINE Developers Console](https://developers.line.biz/console/)

### ✅ Done Criteria
- เปิดผ่าน LINE → เริ่มต้นด้วย `liff.html` → โหลดเว็บได้ปกติ  
- รองรับ Geolocation ใน LINE (โดยใช้ LIFF geolocation API)  
- ไม่มี breaking change ต่อเวอร์ชัน desktop/mobile ที่มีอยู่แล้ว  
